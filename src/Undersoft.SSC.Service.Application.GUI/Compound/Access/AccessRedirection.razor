@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"account/sign_in?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
